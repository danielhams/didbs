#!/bin/sh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
. ./setupenv.sh
cd $BUILDDIR || exit -1
mkdir ../build-libffi
cd ../build-libffi
$BUILDDIR/configure --prefix=$INSTALLDIR --disable-silent-rules || exit -1
exit 0
