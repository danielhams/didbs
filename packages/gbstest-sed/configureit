#!/bin/sh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
. ./setupenv.sh
cd $BUILDDIR || exit -1
#mkdir ../build-sed
#cd ../build-sed
gl_cv_func_select_supports0=no gl_cv_func_select_detects_ebadf=no $BUILDDIR/configure --prefix=$GBS8_INSTALL_DIR --disable-silent-rules || exit -1
exit 0
