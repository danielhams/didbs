diff -u -r -w gdb-7.8.2/gdb/common/agent.c gdb-7.8.2-patched/gdb/common/agent.c
--- gdb-7.8.2/gdb/common/agent.c	2015-01-15 10:58:12.000000000 +0000
+++ gdb-7.8.2-patched/gdb/common/agent.c	2019-04-01 20:43:21.655296120 +0000
@@ -31,6 +31,8 @@
 #include "agent.h"
 #include "filestuff.h"
 
+#define P_tmpdir "/var/tmp"
+
 int debug_agent = 0;
 
 #ifdef GDBSERVER
Only in gdb-7.8.2-patched/gdb/common: agent.c~
diff -u -r -w gdb-7.8.2/gdb/defs.h gdb-7.8.2-patched/gdb/defs.h
--- gdb-7.8.2/gdb/defs.h	2015-01-15 10:58:12.000000000 +0000
+++ gdb-7.8.2-patched/gdb/defs.h	2019-04-01 20:28:59.621323400 +0000
@@ -21,6 +21,10 @@
 #ifndef DEFS_H
 #define DEFS_H
 
+#include <sys/time.h>
+#include <sys/ucontext.h>
+#include <string.h>
+
 #ifdef GDBSERVER
 #  error gdbserver should not include gdb/defs.h
 #endif
@@ -69,6 +73,12 @@
 /* For ``enum gdb_signal''.  */
 #include "gdb/signals.h"
 
+#include <sys/signal.h>
+
+#if !defined(NSIG)
+#define NSIG SIGRTMAX
+#endif
+
 #include "ui-file.h"
 
 #include "host-defs.h"
@@ -756,4 +766,7 @@
 
 #include "utils.h"
 
+extern int strncasecmp(const char *, const char *, size_t );
+extern int strcasecmp(const char *, const char *);
+
 #endif /* #ifndef DEFS_H */
Only in gdb-7.8.2-patched/gdb: defs.h.orig
Only in gdb-7.8.2-patched/gdb: defs.h~
Only in gdb-7.8.2-patched/gdb: symtab.c~
diff -u -r -w gdb-7.8.2/libiberty/strsignal.c gdb-7.8.2-patched/libiberty/strsignal.c
--- gdb-7.8.2/libiberty/strsignal.c	2014-06-11 17:34:41.000000000 +0000
+++ gdb-7.8.2-patched/libiberty/strsignal.c	2019-04-01 20:05:17.100246520 +0000
@@ -248,6 +248,8 @@
 #else
 #ifdef _NSIG
 static int sys_nsig = _NSIG;
+#else
+static int sys_nsig = SIGRTMAX;
 #endif
 #endif
 extern const char * const sys_siglist[];
Only in gdb-7.8.2-patched/libiberty: strsignal.c.orig
diff -u -r -w gdb-7.8.2/readline/signals.c gdb-7.8.2-patched/readline/signals.c
--- gdb-7.8.2/readline/signals.c	2014-06-11 17:34:41.000000000 +0000
+++ gdb-7.8.2-patched/readline/signals.c	2019-04-01 20:05:17.103982600 +0000
@@ -21,6 +21,9 @@
 
 #define READLINE_LIBRARY
 
+#include <sys/time.h>
+#include <sys/signal.h>
+
 #if defined (HAVE_CONFIG_H)
 #  include <config.h>
 #endif
Only in gdb-7.8.2-patched/readline: signals.c.orig
diff -u -r -w gdb-7.8.2/sim/common/sim-arange.c gdb-7.8.2-patched/sim/common/sim-arange.c
--- gdb-7.8.2/sim/common/sim-arange.c	2015-01-15 10:58:12.000000000 +0000
+++ gdb-7.8.2-patched/sim/common/sim-arange.c	2019-04-01 20:05:17.107027080 +0000
@@ -280,11 +280,7 @@
   build_search_tree (ar);
 }
 
-#endif /* DEFINE_NON_INLINE_P */
-
-#if DEFINE_INLINE_P
-
-SIM_ARANGE_INLINE int
+int
 sim_addr_range_hit_p (ADDR_RANGE *ar, address_word addr)
 {
   ADDR_RANGE_TREE *t = ar->range_tree;
@@ -301,4 +297,4 @@
   return 0;
 }
 
-#endif /* DEFINE_INLINE_P */
+#endif /* DEFINE_NON_INLINE_P */
Only in gdb-7.8.2-patched/sim/common: sim-arange.c.orig
diff -u -r -w gdb-7.8.2/sim/common/sim-arange.h gdb-7.8.2-patched/sim/common/sim-arange.h
--- gdb-7.8.2/sim/common/sim-arange.h	2015-01-15 10:58:12.000000000 +0000
+++ gdb-7.8.2-patched/sim/common/sim-arange.h	2019-04-01 20:05:17.109392360 +0000
@@ -62,7 +62,7 @@
 
 /* Return non-zero if ADDR is in range AR, traversing the entire tree.
    If no range is specified, that is defined to mean "everything".  */
-extern INLINE int
+extern int
 sim_addr_range_hit_p (ADDR_RANGE * /*ar*/, address_word /*addr*/);
 #define ADDR_RANGE_HIT_P(ar, addr) \
   ((ar)->range_tree == NULL || sim_addr_range_hit_p ((ar), (addr)))
Only in gdb-7.8.2-patched/sim/common: sim-arange.h.orig
