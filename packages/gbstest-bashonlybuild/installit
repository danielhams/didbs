#!/bin/sh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
. ./setupenv.sh
cd $BUILDDIR || exit -1
cd ../build-bash
#make install || exit -1
#cd $INSTALLDIR/bin
#ln -s -f bash sh || exit -1
exit 0
