#!/bin/sh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
. ./setupenv.sh
cd $BUILDDIR || exit -1
#autoreconf
mkdir ../build-bu
cd ../build-bu
$BUILDDIR/configure --prefix=$GBS9_1_INSTALL_DIR --libdir=$GBS9_1_INSTALL_DIR/$DIDBS_LIBDIR --enable-werror=no --disable-nls --disable-iconv --disable-gprof --with-system-zlib || exit -1

exit 0
