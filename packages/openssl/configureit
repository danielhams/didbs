#!/bin/sh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
. ./setupenv.sh
cd $BUILDDIR || exit -1
./Configure --prefix=$INSTALLDIR --openssldir=$INSTALLDIR/ssl irix-mips4-cc threads shared || exit -1
exit 0
