#!/bin/sh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
LD_LIBRARYN32_PATH="$INSTALLDIR=/lib"
export LD_LIBRARYN32_PATH
cd $BUILDDIR || exit -1
make -j $DIDBS_JOBS || exit -1
make install || exit -1
exit 0
