#!/bin/sh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
. ./setupenv.sh
cd $BUILDDIR || exit -1
cd ../build-gdb
make install || exit -1
# Unneeded libiberty.a
cd $GBSTEST_INSTALL_DIR
rm -rf ./lib32
exit 0
