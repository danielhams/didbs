#!/bin/sh

BUILDDIR=$1
INSTALLDIR=$2
EXTRAARGS=$3
echo $BUILDDIR
echo $INSTALLDIR
echo $EXTRAARGS
. ./setupenv.sh
cd $BUILDDIR || exit -1
am_cv_func_iconv=no ./configure --prefix=$INSTALLDIR --with-ssl=openssl --with-included-libunistring || exit -1
exit 0
